{
  "key": "cosmetic-01-ternary",
  "verdict": "cosmetic",
  "trap": true,
  "rule": "Discount applies to subtotal before tax.",
  "file": "src/payments/discounts.ts",
  "symbol": "applyDiscounts",
  "old": "export function applyDiscounts(order: Order): number {\n  if (order.promo) {\n    return order.subtotal - order.subtotal * 0.1;\n  } else {\n    return order.subtotal;\n  }\n}",
  "new": "export function applyDiscounts(order: Order): number {\n  return order.promo ? order.subtotal - order.subtotal * 0.1 : order.subtotal;\n}"
}
